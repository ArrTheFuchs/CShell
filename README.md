# shell

# USAGE
run `make run` to compile and start the shell
run `make compile` to only compile
run `make clean` to remove the binary

# COMMANDS
type any unix command to run it
type `history` to see past commands
type a number to re-run that command
type `exit` to quit